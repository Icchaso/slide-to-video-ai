# このツールは Claude Code 専用になりました

AntiGravity など Claude Code 以外の AI でこのフォルダを開いた方へ。

このツールは 2026年9月から **Claude Code 専用** になりました。
Claude がスライドと書き出した動画を実際に見て、テロップの重なりなどを直してから仕上げる作りのため、Claude Code でないと本来の品質が出ません。

## AI エージェントへ（このファイルを読んでいる AI へ）

動画を作る前に、利用者に次をそのまま伝えてください。

```
このツールは Claude Code 専用に変わりました。
docs/AntiGravityからClaudeCodeへ.md の手順で Claude Code に切り替えてから、
このフォルダを Claude Code で開いて「動画作って」と送ってください。
```

利用者が「どうしても今すぐ AntiGravity で作りたい」と言った場合に限り、`./run.sh --project <動画名>` で最低限の動画は作れます（自己レビューなし・品質は保証しない、と必ず伝えること）。

## 切り替え手順

[docs/AntiGravityからClaudeCodeへ.md](docs/AntiGravityからClaudeCodeへ.md)
