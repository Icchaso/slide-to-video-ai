{
  "name": "slide-to-video-ai",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix hyperframes-app run dev",
    "check": "npm --prefix hyperframes-app run check",
    "render": "npm --prefix hyperframes-app run render",
    "publish": "npm --prefix hyperframes-app run publish"
  }
}
